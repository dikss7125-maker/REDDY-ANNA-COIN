V4 PostgreSQL: set DATABASE_URL and REQUIRE_DATABASE=true. On a fresh empty database, server.js imports the existing users_data.json/data.json state into app_state automatically. Existing DB state is loaded on restart and is not overwritten by JSON. Verify /api/health after deploy. Keep JSON backups until users/balances are verified.
