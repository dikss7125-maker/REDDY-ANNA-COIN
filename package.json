{
  "name": "mahadev-book-virtual",
  "version": "3.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:casino",
    "build:casino": "cd casino-app && npm install --include=dev --no-audit --no-fund && npm run build",
    "prestart": "npm run build:casino"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.16.3"
  }
}
